/*
In JavaScript, an object is a collection of properties.

A property is a key–value pair (key: value).

Keys (also called properties) are always strings or symbols.

Values can be anything (string, number, boolean, array, function, or another object).
*/

const person = {
  name: "Raju",
  age: 22,
  isStudent: true
};
console.log(person.name);   // "Raju"
console.log(person["age"]); // 22
